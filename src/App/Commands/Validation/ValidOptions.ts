export enum ValidNodeOptions {
    type = 'type',
    position = 'position',
    color = 'color',
    duration = 'duration',
    animation = 'animation',
    radius = 'radius',
    indexing = 'indexing'
}

export enum ValidConnOptions {
    color = 'color',
    duration = 'duration',
    elevation = 'elevation',
    text = 'text'
}

export enum ValidAnimableConfOptions {
    duration = 'duration'
}